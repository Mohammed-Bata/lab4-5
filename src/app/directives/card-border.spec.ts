import { CardBorder } from './card-border';

describe('CardBorder', () => {
  it('should create an instance', () => {
    const directive = new CardBorder();
    expect(directive).toBeTruthy();
  });
});
