import { FormatfourPipe } from './formatfour-pipe';

describe('FormatfourPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatfourPipe();
    expect(pipe).toBeTruthy();
  });
});
